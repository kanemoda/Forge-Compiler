// Header fixture consumed by `include.c`.  Defines a few macros the
// including test can expand and assert on.
#ifndef FORGE_TESTS_VALUES_H
#define FORGE_TESTS_VALUES_H

#define PI 314
#define GREETING "hello, world"
#define DOUBLE(x) ((x) + (x))

#endif

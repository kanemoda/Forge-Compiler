// RUN: forge --version
// CHECK: forge
//
// Verify that the compiler binary starts up and reports a version string
// that contains "forge".  This is the minimal smoke test — if it fails,
// the binary is missing or fundamentally broken.
